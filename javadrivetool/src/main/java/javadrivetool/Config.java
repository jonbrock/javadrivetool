package javadrivetool;

import java.nio.file.Path;

public class Config {
	/* Global config. */
	public static Path root;
	public static Path config;
	
	/* Pull config. */
	public static boolean pullComputeMd5;
}
